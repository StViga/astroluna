[phases.setup]
nixPkgs = ['nodejs_18']

[phases.install]
cmds = [
    'npm ci --only=production --silent --no-audit --no-fund'
]

[phases.build]
cmds = [
    'npm run build'
]

[start]
cmd = 'npm run start:railway'