# Cloudflare Pages redirects for Hono application
# All routes handled by Worker, static assets served directly

# Static assets - served directly by Pages
/static/* 200

# All other routes (including root /) - handled by Worker
/* 200